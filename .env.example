HOST=localhost
PORT=3000
#Do not change above Host and Port values , they are required for the application to run correctly.

#postgresql database configuration
PGUSER=postgres
PGHOST=localhost
PGDATABASE=starX
PGPASSWORD=123456
PGPORT=1234 

#jwt secret key
JWT_SECRET=starX

#redis configuration
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
