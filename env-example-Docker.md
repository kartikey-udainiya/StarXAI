# Backend server URL (used by Socket + API)
SERVER_URL=http://localhost:3000

# Express server port
PORT=3000

# PostgreSQL database configuration
PGUSER=postgres
PGHOST=db         # <— PostgreSQL docker service name
PGDATABASE=starX
PGPASSWORD=123456
PGPORT=5432       # Default Postgres port

# JWT secret key
JWT_SECRET=starX

# Redis configuration
REDIS_HOST=redis  # <— Redis docker service name
REDIS_PORT=6379
